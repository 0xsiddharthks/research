# Encrypt everything by default
* filter=git-crypt diff=git-crypt

# Keep these unencrypted (readable on GitHub)
LICENSE !filter !diff
README.md !filter !diff
.gitattributes !filter !diff
.gitignore !filter !diff
.claude/** !filter !diff
.git-crypt/** !filter !diff
